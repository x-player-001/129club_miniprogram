<!-- 个人中心页面 -->
<view class="profile-container">
  <!-- 游客模式：登录引导 -->
  <view wx:if="{{!isLogin}}" class="guest-guide">
    <view class="guest-card">
      <text class="guest-icon">👋</text>
      <text class="guest-title">欢迎来到129俱乐部</text>
      <text class="guest-text">登录后可查看个人数据、参与比赛报名等更多功能</text>
      <button class="login-btn" bindtap="onGoToLogin">立即登录</button>
    </view>

    <!-- 游客可用功能 -->
    <view class="menu-section">
      <view class="menu-title">可用功能</view>
      <view class="menu-list">
        <menu-item icon="{{icons.notice}}" text="通知公告" type="notice" bind:tap="onMenuItemTap"></menu-item>
        <menu-item icon="{{icons.setting}}" text="关于我们" type="about" bind:tap="onMenuItemTap"></menu-item>
      </view>
    </view>
  </view>

  <!-- 登录模式：完整个人中心 -->
  <view wx:else>
    <!-- 顶部背景 -->
    <view class="profile-header">
      <view class="header-bg"></view>

      <!-- 用户信息 -->
      <view class="user-card">
      <image class="avatar" src="{{userInfo.avatar || images.defaultAvatar}}" mode="aspectFill"></image>
      <view class="user-info">
        <view class="user-name-row">
          <text class="user-name">{{userInfo.realName || userInfo.nickname || '未设置'}}</text>
          <text class="user-nickname" wx:if="{{userInfo.nickname && userInfo.realName}}"> @{{userInfo.nickname}}</text>
        </view>
        <view class="user-meta">
          <text class="jersey-number" wx:if="{{userInfo.jerseyNumber}}">{{userInfo.jerseyNumber}}号</text>

          <!-- 显示所有位置 -->
          <block wx:if="{{userInfo.positions && userInfo.positions.length > 0}}">
            <position-tag wx:for="{{userInfo.positions}}" wx:key="*this" code="{{item}}" size="small"></position-tag>
          </block>

          <!-- 惯用脚展示 -->
          <foot-skill-display
            leftFootSkill="{{userInfo.leftFootSkill}}"
            rightFootSkill="{{userInfo.rightFootSkill}}">
          </foot-skill-display>
        </view>
      </view>
      <view class="edit-btn" bindtap="onEditProfile">
        <image src="{{icons.edit}}" mode="aspectFit"></image>
      </view>
    </view>

    <!-- 队伍信息 -->
    <view class="team-info-card" wx:if="{{currentTeam}}" bindtap="onGoToTeamDetail">
      <view class="team-badge">
        <image src="{{currentTeam.logo || images.defaultTeam}}" mode="aspectFit"></image>
      </view>
      <view class="team-text">
        <text class="team-name">{{currentTeam.name}}</text>
        <text class="team-role">{{currentTeam.iscaptain ? '队长' : '队员'}}</text>
      </view>
      <image class="arrow" src="{{icons.arrowRightWhite}}" mode="aspectFit"></image>
    </view>

    <!-- 数据统计卡片 -->
    <view class="stats-card">
      <view class="stats-row">
        <view class="stat-item">
          <text class="stat-value">{{personalStats.matches || 0}}</text>
          <text class="stat-label">出场</text>
        </view>
        <view class="stat-divider"></view>
        <view class="stat-item">
          <text class="stat-value">{{personalStats.goals || 0}}</text>
          <text class="stat-label">进球</text>
        </view>
        <view class="stat-divider"></view>
        <view class="stat-item">
          <text class="stat-value">{{personalStats.assists || 0}}</text>
          <text class="stat-label">助攻</text>
        </view>
        <view class="stat-divider"></view>
        <view class="stat-item">
          <text class="stat-value">{{personalStats.mvpCount || 0}}</text>
          <text class="stat-label">MVP</text>
        </view>
      </view>
      <view class="view-detail" bindtap="onViewStats">
        <text>查看详细数据</text>
        <image src="{{icons.arrowRight}}" mode="aspectFit"></image>
      </view>
    </view>

    <!-- 功能菜单 - 使用 menu-item 组件 -->
    <view class="menu-section">
      <view class="menu-title">我的功能</view>
      <view class="menu-list">
        <menu-item
          icon="{{icons.users}}"
          text="队员列表"
          type="members"
          bind:tap="onMenuItemTap">
        </menu-item>

        <menu-item
          icon="{{icons.message}}"
          text="消息中心"
          type="message"
          badge="{{unreadCount}}"
          bind:tap="onMenuItemTap">
        </menu-item>

        <menu-item
          icon="{{icons.notice}}"
          text="通知公告"
          type="notice"
          bind:tap="onMenuItemTap">
        </menu-item>
      </view>
    </view>

    <view class="menu-section">
      <view class="menu-title">设置</view>
      <view class="menu-list">
        <menu-item
          icon="{{icons.setting}}"
          text="设置"
          type="settings"
          bind:tap="onMenuItemTap">
        </menu-item>
      </view>
    </view>
  </view>

  <!-- 版本信息 -->
  <view class="version-info">
    <text>版本 {{version}}</text>
  </view>

  <!-- 底部占位 -->
  <view class="bottom-space"></view>
</view>
